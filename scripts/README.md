## Debug Scripts

The `scripts/` folder contains small helper scripts used during development
to validate specific components (domain, cache, client, etc).

They are not part of the production system.